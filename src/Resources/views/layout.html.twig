<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
    <title>{% block title %}Iniciar sesión{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('components/admin-lte/bootstrap/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/easyadmin/stylesheet/font-awesome.min.css') }}">
        <link rel="stylesheet" href="{{ asset('components/admin-lte/dist/css/AdminLTE.min.css') }}">
        <link rel="stylesheet" href="{{ asset('components/admin-lte/dist/css/skins/skin-blue.min.css') }}">
        <link rel="stylesheet" href="{{ asset('components/admin-lte/plugins/iCheck/square/blue.css') }}">
        <style>
            .login-box-body .form-control-feedback, .register-box-body .form-control-feedback {
                padding-top: 10px;
            }
        </style>
    {% endblock %}
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body class="login-page">

{% if app.request.hasPreviousSession %}
    {% for type, messages in app.session.flashbag.all() %}
        {% for message in messages %}
            <div class="flash-{{ type }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
{% endif %}

<div class="login-box">
    <div class="login-logo">
        <a href="{{ path("homepage") }}">{{ site_title | raw }}</a>
    </div><!-- /.login-logo -->
    {% block fos_user_content %}
    {% endblock fos_user_content %}
</div>
{% block javascripts %}
    <!-- jQuery 2.1.4 -->
    <script src="{{ asset('components/admin-lte/plugins/jQuery/jQuery-2.1.4.min.js') }}" type="text/javascript"></script>
    <!-- Bootstrap 3.3.2 JS -->
    <script src="{{ asset('components/admin-lte/bootstrap/js/bootstrap.min.js') }}" type="text/javascript"></script>
    <!-- iCheck -->
    <script src="{{ asset('components/admin-lte/plugins/iCheck/icheck.min.js') }}" type="text/javascript"></script>
    <script>
        $(function () {
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' // optional
            });
        });
    </script>
{% endblock %}
</body>
</html>
